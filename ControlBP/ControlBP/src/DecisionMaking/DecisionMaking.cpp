#include <Arduino.h>
#include "DecisionMaking/DecisionMaking.h"

/**
 * Picks the post to find
 * NEBULOUS STUFF RN
 */
int pick_post_to_find()
{
    Serial.println("pick_post_to_find: To the left, to the left!");
    return 0;
}

/**
 * Says which side we expect checkpoint on
 */
int get_checkpoint_expected_side()
{
    Serial.println("get_checkpoint_expected_side: LEFT");
    return 0;
}

/**
 * what it says on the tin, gotta think about it
 */
void update_position()
{
    Serial.println("update position: updated!");
}